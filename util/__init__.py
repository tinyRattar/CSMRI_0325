from .imageUtil import *
from .recoder import *

def paramNumber(net):
    params = list(net.parameters())
    k = 0
    for i in params:
        l = 1
        for j in i.size():
            l *= j
        k = k + l
    
    return k